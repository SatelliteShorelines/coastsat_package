on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

name: Test on Multiple OS with Python 3.9
jobs:
  Test:
    name: ${{ matrix.os }}, ${{ matrix.env }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        env:
          - ci/envs/39-coastseg.yaml
          - ci/envs/38-coastseg.yaml
        include:
          - env: ci/envs/39-coastseg.yaml
            os: macos-latest
          - env: ci/envs/39-coastseg.yaml
            os: windows-latest
          - env: ci/envs/39-coastseg.yaml
            os: ubuntu-latest
          - env: ci/envs/38-coastseg.yaml
            os: macos-latest
          - env: ci/envs/38-coastseg.yaml
            os: windows-latest
          - env: ci/envs/38-coastseg.yaml
            os: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Conda with python 3.9
        uses: conda-incubator/setup-miniconda@v2
        with:
          environment-file: ${{ matrix.env }}
          miniconda-version: "latest"
      - run: |
          conda info
          conda list
      - name: Install Latest coastsat-package
        run: |
          python -m pip install --upgrade pip
          pip install coastsat-package -U